services:
  - type: web
    name: veeb-resolver
    runtime: docker
    # Uploaded baseline. Free sleeps on idle and cannot meet the startup target.
    # Select an always-on compute plan before a production Blueprint sync.
    plan: free
    healthCheckPath: /
    envVars:
      - key: RESOLVER_SECRET
        sync: false
