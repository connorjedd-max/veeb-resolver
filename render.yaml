services:
  - type: web
    name: veeb-youtube-resolver
    runtime: docker
    plan: free
    dockerfilePath: ./Dockerfile
    healthCheckPath: /health
    envVars:
      - key: RESOLVER_SECRET
        sync: false
      - key: RESOLVER_CACHE_TTL
        value: "600"
